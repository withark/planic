import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: '참고 자료 · 플래닉 Planic',
}

export default function ReferenceEstimateLayout({ children }: { children: React.ReactNode }) {
  return children
}
